syntax = "proto3";

package pluginassistanepb;

// NoteCommand - note command
message NoteCommand {
    // keys
    repeated string keys = 1;
}

// ExpNotesCommand - expnotes command
message ExpNotesCommand {
    // graph mode
    bool graph = 1;
}